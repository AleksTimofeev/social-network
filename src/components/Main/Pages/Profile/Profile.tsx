import React from 'react';
import {Grid} from "@mui/material";

const Profile = () => {
  return (
    <Grid container spacing={2}>
      <Grid item xs={8}>
          <h2>Profile</h2>
      </Grid>
    </Grid>
  );
};

export default Profile;